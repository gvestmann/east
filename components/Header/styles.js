import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        top: 50,
        zIndex: 99,
    },

    logo: {
        width: 175,
        height: 67,
        resizeMode: 'contain'
    },
})

export default styles